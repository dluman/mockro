version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    post_install:
      - pip install mkdocs-material mkdocstrings[python]

python:
  install:
    - method: pip
      path: .

mkdocs:
  configuration: mkdocs.yml

formats: []
